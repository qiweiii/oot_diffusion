build:
  gpu: true
  cuda: "12.2"
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
    - "ninja-build"
  python_version: "3.10"
  python_packages:
    - "ninja==1.10.2"
    - "torch==2.2.0"
    # note: branch or commit or tag is required, or it won't update
    - "git+https://github.com/qiweiii/oot_diffusion.git@main"
predict: "predict.py:Predictor"
