build:
  gpu: true
  cuda: "12.2"
  system_packages:
    - "libgl1-mesa-glx"
    - "libglib2.0-0"
    - "ninja-build"
  python_version: "3.10"
  python_packages:
    - "ninja==1.10.2"
    - "torch==2.2.0"
    - "git+https://github.com/qiweiii/oot_diffusion.git@main"
predict: "predict.py:Predictor"
# NOTE: may need to do `docker buildx prune -f` first to make sure it updates...
# until this is fixed and my cog upgraded: https://github.com/replicate/cog/issues/1620
